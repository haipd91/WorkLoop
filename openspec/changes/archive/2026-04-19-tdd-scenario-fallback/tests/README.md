# Tests placeholder

Change này sửa contract của `/tdd`, không phải output test thực thi cho một feature
cụ thể.

Artifact `tests` được giữ ở mức placeholder để schema coi change là apply-ready.
Khi implement change này, có thể thêm examples hoặc fixture files nếu cần minh họa
scenario mode và runnable mode.
